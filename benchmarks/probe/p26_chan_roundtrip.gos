use std::sync

fn produce(tx: Sender<i64>) {
    tx.send(7i64)
    tx.close()
}

fn main() {
    let tx, rx = sync::channel::<i64>()
    spawn(|| produce(tx))
    let x = rx.recv().unwrap_or(0i64)
    println("{}", x)
}

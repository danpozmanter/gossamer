# `std::http::binary_bodies`

Status: shipped

NUL-safe packed byte request and response bodies through `Request.raw_body` and
`Response.raw_bytes`.

This status records availability. It is not a Stable compatibility promise.

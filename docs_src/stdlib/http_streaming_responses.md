# `std::http::streaming_responses`

Status: shipped

Chunked server responses and incremental client reads through
`ResponseStream::next_chunk`.

This status records availability. It is not a Stable compatibility promise.

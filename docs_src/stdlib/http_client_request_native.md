# `std::http::client_request_native`

Status: shipped

Native compiled-tier implementation of `http::request` and
`http::request_bytes` through the shared HTTP client engine.

This status records availability. It is not a Stable compatibility promise.

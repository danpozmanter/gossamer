# `tooling::faithful_fmt`

Status: shipped

Token-stream `gos fmt` preserves comments and macros, checks idempotence, and
refuses destructive rewrites.

This status records availability. It is not a Stable compatibility promise.

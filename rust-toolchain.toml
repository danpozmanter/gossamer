[toolchain]
channel = "1.95.0"
components = ["rustfmt", "clippy"]
# Release builds use the musl target for fully-static binaries
# (`gos build --release`). Declared here so a fresh checkout
# `cargo` invocation auto-installs it via rustup.
targets = ["x86_64-unknown-linux-musl"]

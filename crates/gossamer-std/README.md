# gossamer-std

The Gossamer standard library. Source files live under `src/` and are
written in Gossamer itself, compiled by the `gos` toolchain.

This directory is intentionally not a Rust crate and is not a member of
the workspace. It is bundled as data at build time once Begins.

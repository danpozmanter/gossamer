// generated by gos build -- do not edit
// fingerprint: {{ fingerprint_hex }}

{{#each bindings}}
extern crate {{ this.crate_name_ident }};
{{/each}}

/// Single C entry point the user's compiled main() must call
/// before any binding dispatch. Idempotent.
#[unsafe(no_mangle)]
pub extern "C" fn gos_static_install_bindings() {
    static ONCE: std::sync::Once = std::sync::Once::new();
    ONCE.call_once(|| {
        {{#each bindings}}
        {{ this.crate_name_ident }}::__bindings_force_link();
        {{/each}}
        gossamer_binding::install_all_for_compiled();
    });
}

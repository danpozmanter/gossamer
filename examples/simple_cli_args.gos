/*
  * Simple Command-line argument parsing.
  *
  * Run:
  *     gos run examples/simple_cli_args.gos -- alpha beta
  */

use std::{iter, os}

fn main() {
  println!("program: {}", os::program_name())
  os::args() |> iter::for_each(|a| println!("arg: {}", a))
}


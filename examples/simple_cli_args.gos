/*
  * Simple Command-line argument parsing.
  *
  * Run:
  *     gos run examples/simple_cli_args.gos alpha beta
  */

use std::{env, iter}

fn main() {
    println!("program: {}", env::program_name())
    env::args() |> iter::for_each(|a| println!("arg: {}", a))
}

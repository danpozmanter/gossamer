/*
 * Canonical first program.
 *
 * Output goes through the `println!` macro - no `use` declaration
 * is required. The macro interpolates captured identifiers and
 * `{}` placeholders: `println!("hello, {name}")`.
 */

fn main() {
    println!("hello, world")
}

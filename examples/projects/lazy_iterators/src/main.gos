use std::iter

fn main() {
    let result = (0..20)
        |> iter::map(|x| x * 3, $)
        |> iter::filter(|x| x % 2 == 0, $)
        |> iter::take(5, $)
        |> iter::collect
    println!("{:?}", result)
}

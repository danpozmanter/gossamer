use std::iter

fn main() {
    let result = (0..20)
        |> |v| iter::map(|x| x * 3, v)
        |> |v| iter::filter(|x| x % 2 == 0, v)
        |> |v| iter::take(5, v)
        |> iter::collect
    println!("{:?}", result)
}

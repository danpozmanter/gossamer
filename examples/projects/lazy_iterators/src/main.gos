use std::iter

fn main() {
    let result = (0..20)
        |> |v| iter::map(v, |x| x * 3)
        |> |v| iter::filter(v, |x| x % 2 == 0)
        |> |v| iter::take(v, 5)
        |> iter::collect
    println("{:?}", result)
}

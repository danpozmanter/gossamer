/*
 * Basic interactive input.
 *
 * Run:
 *     gos run examples/input.gos
 */

use std::io

let mut input = String::new()
println("Type something: ")

io::stdin()
    .read_line(&mut input)
    .unwrap()

let result = input.trim()
println("You typed: {}", result)

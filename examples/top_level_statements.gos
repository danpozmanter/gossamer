// The entry file is implicitly `fn main()`: these statements are its body,
// and `double` is hoisted out as an ordinary item.
fn double(n: i64) -> i64 { n * 2 }

let nums = [1, 2, 3]
for n in nums {
    println!("{} -> {}", n, double(n))
}

use std::runtime
use std::time

fn my_hook(msg: String) {
    println("custom hook saw: {}", msg)
}

fn boom() {
    panic("hooked panic")
}

fn main() {
    runtime::set_panic_hook(my_hook)
    spawn(|| boom())
    time::sleep(100)
    println("done")
}

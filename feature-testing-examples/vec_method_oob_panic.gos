// Vec insert/remove are invariant operations in method and qualified form:
// invalid indices panic on every tier.

fn main() {
    let mut xs = Vec::from([1, 2, 3])
    xs.insert(3, 4)
    println!("before {}", xs.len())
    xs.insert(5, 99)
    println!("unreachable")
}

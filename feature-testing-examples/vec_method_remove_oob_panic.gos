// Vec remove panics on invalid indices in method and qualified forms.

fn main() {
    let mut xs = Vec::from([1, 2, 3])
    xs.remove(2)
    println!("before {}", xs.len())
    xs.remove(2)
    println!("unreachable")
}

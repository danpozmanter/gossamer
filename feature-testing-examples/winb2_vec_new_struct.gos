struct S { a: i64, b: String }

fn main() {
    let mut v: Vec<S> = Vec::new()
    v.push(S(1, "x"))
    v.push(S(2, "yy"))
    v.push(S(3, "zzz"))
    for s in v {
        println!("{} {}", s.a, s.b)
    }
    let lit: Vec<S> = []
    println!("lit_len={}", lit.len())
}

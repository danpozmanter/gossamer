// Positional field copying from an existing struct across all three tiers.

struct Config {
    host: String,
    port: i64,
    verbose: bool,
    retries: i64,
}

fn default_config() -> Config {
    Config { host: "localhost", port: 8080, verbose: false, retries: 3 }
}

fn main() {
    let base = default_config()
    let custom = Config { host: base.host, port: 9090, verbose: true, retries: base.retries }
    println!("host = {}", custom.host)
    println!("port = {}", custom.port)
    println!("verbose = {}", custom.verbose)
    println!("retries = {}", custom.retries)
    let extra = Config { host: "example.com", port: custom.port, verbose: custom.verbose, retries: custom.retries }
    println!("host2 = {}", extra.host)
    println!("port2 = {}", extra.port)
    println!("verbose2 = {}", extra.verbose)
    println!("retries2 = {}", extra.retries)
}
